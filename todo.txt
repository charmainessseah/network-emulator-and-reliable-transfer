work done:

in sender.py:
- modified parse_command_line_args()

in requester.py:
- modified parse_command_line_args()


clarification questions:
- outer packet length (is it in bytes) ?

main method:

# inputs = [sock]
# outputs = [sock]

# readable,writable,exceptional = select.select(inputs,outputs,inputs,0.5)

    # for s in writable:
    #     # data = sock.send(b'hello\r\n')
    #     header = struct.pack('!cII', 'D'.encode('ascii'), 1, 10)
    #     data = 'Hello world'.encode('ascii')
    #     packet_with_header = header + data
    #     sock.sendto(packet_with_header, ('Charmaines-MacBook-Pro.local', 1234))
    #     outputs.remove(sock)

    # for s in readable:
    #     data = sock.recv(1024)
    #     sock.close()
    #     inputs.remove(sock)
    #     break

    # for s in exceptional:
    #     inputs.remove(sock)
    #     outputs.remove(sock)
    #     break
